<div dir="rtl">

# 🎨 سیستم طراحی کتابخانه خوانش گستر

## مستند جامع معماری بصری و توکن‌های طراحی (Design System Documentation)

> **نسخه:** ۱.۰.۰  
> **نویسنده:** محمد حیدری (Mohammad Heydari)  
> **آخرین به‌روزرسانی:** ۲۶ مرداد ۱۴۰۵  
> **لایسنس:** MIT

---

## 📌 فهرست مطالب

- [🎨 سیستم طراحی کتابخانه خوانش گستر](#-سیستم-طراحی-کتابخانه-خوانش-گستر)
  - [مستند جامع معماری بصری و توکن‌های طراحی (Design System Documentation)](#مستند-جامع-معماری-بصری-و-توکنهای-طراحی-design-system-documentation)
  - [📌 فهرست مطالب](#-فهرست-مطالب)
  - [مقدمه و فلسفه طراحی](#مقدمه-و-فلسفه-طراحی)
  - [توکن‌های طراحی (Design Tokens)](#توکنهای-طراحی-design-tokens)
  - [پالت رنگی (Color Palette)](#پالت-رنگی-color-palette)
    - [تم روشن (Light Theme)](#تم-روشن-light-theme)
    - [تم تاریک (Dark Theme)](#تم-تاریک-dark-theme)
    - [نقش‌های رنگی (Color Roles)](#نقشهای-رنگی-color-roles)
      - [سلسله‌مراتب رنگی برای حالت‌های تعاملی](#سلسلهمراتب-رنگی-برای-حالتهای-تعاملی)
  - [تایپوگرافی (Typography)](#تایپوگرافی-typography)
    - [فونت (Font Family)](#فونت-font-family)
    - [مقیاس اندازه (Type Scale)](#مقیاس-اندازه-type-scale)
    - [وزن فونت (Font Weights)](#وزن-فونت-font-weights)
  - [سیستم فاصله‌گذاری (Spacing System)](#سیستم-فاصلهگذاری-spacing-system)
    - [توکن‌های ساختاری](#توکنهای-ساختاری)
  - [شعاع‌های گوشه (Border Radius)](#شعاعهای-گوشه-border-radius)
  - [انتقال‌ها (Transitions)](#انتقالها-transitions)
  - [لایه‌بندی (Z-Index Scale)](#لایهبندی-z-index-scale)
  - [سایه‌ها (Shadows)](#سایهها-shadows)
    - [تم روشن](#تم-روشن)
    - [تم تاریک](#تم-تاریک)
  - [شکست‌های واکنش‌گرا (Responsive Breakpoints)](#شکستهای-واکنشگرا-responsive-breakpoints)
    - [استراتژی: موبایل-اول (Mobile-First)](#استراتژی-موبایل-اول-mobile-first)
    - [نقاط شکست (Breakpoints)](#نقاط-شکست-breakpoints)
    - [خلاصه تغییرات چیدمان در هر بریکپوینت](#خلاصه-تغییرات-چیدمان-در-هر-بریکپوینت)

---

## مقدمه و فلسفه طراحی

سیستم طراحی «کتابخانه خوانش گستر» بر اساس استعاره **«کتابخانه فیزیکی»** بنا شده است. هر رنگ، فاصله و بافت بصری در این سیستم، بازتابی از عناصر ملموس یک کتابخانه واقعی است:

- **کاغذهای کاهی و کرم‌رنگ** → پس‌زمینه‌های روشن (`--bg-primary`)
- **قفسه‌های چوبی و جنگلی** → رنگ‌های سبز خزه‌ای (`--moss`)
- **جلدهای طلایی و چرمی کتاب‌ها** → رنگ‌های کهربایی (`--amber`)
- **نور ملایم چراغ مطالعه** → هاله‌های نوری (`--glow-*`)
- **کتابخانه در شب** → تم تاریک با کنتراست بالا

این سیستم طراحی با استفاده از **متغیرهای CSS (Custom Properties)** پیاده‌سازی شده است که امکان مدیریت یکپارچه، تغییر آسان تم‌ها و نگهداری بلندمدت را فراهم می‌آورد.

---

## توکن‌های طراحی (Design Tokens)

تمامی مقادیر بصری پروژه از طریق **متغیرهای سراسری CSS** (CSS Custom Properties) در بلوک `:root` و تم‌های `[data-theme="light"]` و `[data-theme="dark"]` تعریف شده‌اند. این متغیرها به عنوان **منبع واحد حقیقت (Single Source of Truth)** عمل می‌کنند.

> ⚠️ **نکته مهم:** هرگونه تغییر در هویت بصری پروژه باید از طریق این متغیرها اعمال شود. استفاده از مقادیر سخت‌کد (Hardcoded) در سایر بخش‌های CSS اکیداً توصیه نمی‌شود.

---

## پالت رنگی (Color Palette)

### تم روشن (Light Theme)

> 🌞 **استعاره:** نور خورشید بر قفسه‌های طلایی

تم روشن، حالت پیش‌فرض پروژه است و فضایی گرم، دعوت‌کننده و الهام‌گرفته از کاغذ و چوب ایجاد می‌کند.

| توکن               | مقدار                       | نقش                | توضیح                                    |
| ------------------ | --------------------------- | ------------------ | ---------------------------------------- |
| `--bg-primary`     | `#f7f2e2`                   | پس‌زمینه اصلی      | رنگ کاغذ کاهی گرم                        |
| `--bg-secondary`   | `#efe6cd`                   | پس‌زمینه ثانویه    | سایه‌روشن ملایم برای تمایز بلوک‌ها       |
| `--bg-tertiary`    | `#e5dabb`                   | پس‌زمینه سطح سوم   | برای هاور و حالت‌های فعال                |
| `--card-bg`        | `rgba(255, 253, 244, 0.66)` | پس‌زمینه کارت‌ها   | شفافیت برای افکت شیشه‌ای (Glassmorphism) |
| `--card-bg-solid`  | `#fffdf4`                   | پس‌زمینه کارت جامد | بدون شفافیت برای عناصر بحرانی            |
| `--text-primary`   | `#322a18`                   | متن اصلی           | قهوه‌ای تیره (جوهر)                      |
| `--text-secondary` | `#5a4c30`                   | متن ثانویه         | قهوه‌ای متوسط برای پاراگراف‌ها           |
| `--text-muted`     | `#8b7c58`                   | متن کم‌رنگ         | برای توضیحات و متادیتا                   |
| `--text-inverted`  | `#fbf7ea`                   | متن معکوس          | برای متن روی پس‌زمینه‌های تیره           |

| توکن           | مقدار     | نقش                | توضیح                          |
| -------------- | --------- | ------------------ | ------------------------------ |
| `--amber`      | `#c99b3f` | رنگ تأکیدی اصلی    | کهربایی طلایی (جلد کتاب)       |
| `--amber-soft` | `#e8c56a` | کهربایی ملایم      | هاله‌ها و تزئینات              |
| `--amber-deep` | `#8a6410` | کهربایی عمیق       | متن‌های تأکیدی و لوگو          |
| `--moss`       | `#4c7a3d` | رنگ اصلی (Primary) | سبز خزه‌ای (قفسه چوبی)         |
| `--moss-deep`  | `#39602c` | سبز عمیق           | هاور و حالت‌های فعال           |
| `--moss-soft`  | `#8fbf6f` | سبز ملایم          | آیکون‌ها و تزئینات             |
| `--teal`       | `#178f86` | رنگ اطلاعاتی       | فیروزه‌ای (نشانگرها و لینک‌ها) |
| `--teal-soft`  | `#59d3c4` | فیروزه‌ای ملایم    | گرادیان‌ها و هاله‌ها           |
| `--clay`       | `#b0562f` | رنگ هشدار/تعطیلی   | سفالی (برچسب «تعطیل»)          |

### تم تاریک (Dark Theme)

> 🌙 **استعاره:** کتابخانه در شب

تم تاریک با کنتراست بالا و رنگ‌های اشباع‌شده، تجربه مطالعه در محیط‌های کم‌نور را شبیه‌سازی می‌کند.

| توکن               | مقدار                   | نقش                | توضیح                          |
| ------------------ | ----------------------- | ------------------ | ------------------------------ |
| `--bg-primary`     | `#10140d`               | پس‌زمینه اصلی      | سبز-مشکی بسیار تیره            |
| `--bg-secondary`   | `#1a1f14`               | پس‌زمینه ثانویه    | سایه‌روشن ملایم                |
| `--bg-tertiary`    | `#232a1c`               | پس‌زمینه سطح سوم   | هاور و حالت‌های فعال           |
| `--card-bg`        | `rgba(30, 36, 22, 0.6)` | پس‌زمینه کارت‌ها   | شفافیت برای افکت شیشه‌ای       |
| `--card-bg-solid`  | `#1e2416`               | پس‌زمینه کارت جامد | بدون شفافیت                    |
| `--text-primary`   | `#f2ecd9`               | متن اصلی           | کرم روشن                       |
| `--text-secondary` | `#d6cdb2`               | متن ثانویه         | کرم متوسط                      |
| `--text-muted`     | `#a2977c`               | متن کم‌رنگ         | توضیحات و متادیتا              |
| `--text-inverted`  | `#14180e`               | متن معکوس          | برای متن روی پس‌زمینه‌های روشن |

| توکن           | مقدار     | نقش                | توضیح                                  |
| -------------- | --------- | ------------------ | -------------------------------------- |
| `--amber`      | `#e6c15c` | رنگ تأکیدی اصلی    | کهربایی روشن‌تر برای کنتراست در تاریکی |
| `--amber-soft` | `#f0d488` | کهربایی ملایم      | هاله‌ها و تزئینات                      |
| `--amber-deep` | `#c99b3f` | کهربایی عمیق       | متن‌های تأکیدی                         |
| `--moss`       | `#8fbf6f` | رنگ اصلی (Primary) | سبز روشن‌تر برای خوانایی در تاریکی     |
| `--moss-deep`  | `#6ea254` | سبز عمیق           | هاور و حالت‌های فعال                   |
| `--moss-soft`  | `#a4d189` | سبز ملایم          | آیکون‌ها و تزئینات                     |
| `--teal`       | `#59d3c4` | رنگ اطلاعاتی       | فیروزه‌ای روشن‌تر                      |
| `--teal-soft`  | `#7fe3d6` | فیروزه‌ای ملایم    | گرادیان‌ها و هاله‌ها                   |
| `--clay`       | `#e08b5f` | رنگ هشدار/تعطیلی   | سفالی روشن‌تر                          |

### نقش‌های رنگی (Color Roles)

هر رنگ در سیستم طراحی یک **نقش عملکردی** مشخص دارد. این نقش‌ها تعیین می‌کنند که هر رنگ در کجا و چگونه استفاده شود:

| نقش                  | توکن                                | کاربرد                         | مثال در پروژه                            |
| -------------------- | ----------------------------------- | ------------------------------ | ---------------------------------------- |
| **Accent**           | `--accent` → `--amber`              | اقدامات اصلی (CTA)، تأکید بصری | دکمه «عضویت در کتابخانه»، نشانگرهای فعال |
| **Primary**          | `--primary` → `--moss`              | عناصر برند، لینک‌های اصلی      | دکمه «مشاهده خدمات»، سرستون جدول         |
| **Informational**    | `--teal`                            | اطلاعات، زمان‌ها، نشانگرها     | ساعت کاری در کارت خدمات، نوار پیشرفت     |
| **Warning / Closed** | `--clay`                            | هشدار، وضعیت تعطیلی            | برچسب «تعطیل» در جدول ساعات              |
| **Surface**          | `--bg-*`, `--card-bg`               | سطوح و پس‌زمینه‌ها             | بدنه صفحه، کارت‌ها، هدر                  |
| **Text**             | `--text-*`                          | سطوح متنی                      | عناوین، پاراگراف‌ها، توضیحات             |
| **Border**           | `--border-color`, `--border-strong` | حاشیه‌ها و جداسازها            | قاب کارت‌ها، خطوط جدول                   |
| **Glow**             | `--glow-*`                          | هاله‌های نوری تزئینی           | پس‌زمینه بدنه، نوار نقل‌قول، فوتر        |

#### سلسله‌مراتب رنگی برای حالت‌های تعاملی

```

حالت عادی → --accent / --primary
حالت هاور → --accent-hover / --primary-hover
حالت فعال → --amber-deep / --moss-deep
حالت غیرفعال → --text-muted (با شفافیت)

```

---

## تایپوگرافی (Typography)

### فونت (Font Family)

| ویژگی                 | مقدار                              |
| --------------------- | ---------------------------------- |
| **فونت اصلی**         | `Vazirmatn` (وزیرمتن)              |
| **فونت‌های جایگزین**  | `Segoe UI`, `Tahoma`, `sans-serif` |
| **منبع بارگذاری**     | Google Fonts (CDN)                 |
| **استراتژی بارگذاری** | `display=swap` (جلوگیری از FOIT)   |
| **محدوده وزن**        | `100..900` (متغیر - Variable Font) |
| **توکن**              | `--font-family`                    |

> **دلیل انتخاب وزیرمتن:** این فونت، استاندارد صنعتی برای تایپوگرافی وب فارسی است. طراحی آن بر اساس اصول خوانایی در اندازه‌های کوچک و بزرگ بهینه شده و از ارقام فارسی به صورت بومی پشتیبانی می‌کند.

### مقیاس اندازه (Type Scale)

مقیاس تایپوگرافی پروژه بر اساس نسبت **۱.۲۵ (Major Third)** طراحی شده است که تعادل بصری مناسبی بین سطوح مختلف متنی ایجاد می‌کند.

| توکن               | مقدار      | معادل پیکسل | کاربرد                               |
| ------------------ | ---------- | ----------- | ------------------------------------ |
| `--font-size-xs`   | `0.75rem`  | ۱۲px        | برچسب‌های کوچک، حق نشر               |
| `--font-size-sm`   | `0.875rem` | ۱۴px        | متن ثانویه، متادیتا، لینک‌های ناوبری |
| `--font-size-base` | `1rem`     | ۱۶px        | متن بدنه پیش‌فرض                     |
| `--font-size-lg`   | `1.125rem` | ۱۸px        | متن‌های برجسته، متن لودینگ           |
| `--font-size-xl`   | `1.25rem`  | ۲۰px        | عناوین کارت‌ها، زیرعنوان‌ها          |
| `--font-size-2xl`  | `1.5rem`   | ۲۴px        | عناوین سکشن‌ها (موبایل)              |
| `--font-size-3xl`  | `1.875rem` | ۳۰px        | عناوین سکشن‌ها (تبلت)                |
| `--font-size-4xl`  | `2.25rem`  | ۳۶px        | عناوین سکشن‌ها (دسکتاپ)              |
| `--font-size-5xl`  | `3rem`     | ۴۸px        | عنوان اصلی صفحه (دسکتاپ بزرگ)        |

> **نکته فنی:** برای عناوین اصلی از تابع `clamp()` استفاده شده است که تایپوگرافی سیال (Fluid Typography) را بدون نیاز به Media Queryهای متعدد ممکن می‌سازد:
>
> ```css
> font-size: clamp(var(--font-size-2xl), 5vw, var(--font-size-4xl));
> ```

### وزن فونت (Font Weights)

| توکن                     | مقدار | کاربرد                                  |
| ------------------------ | ----- | --------------------------------------- |
| `--font-weight-light`    | `300` | متن نقل‌قول انگلیسی، نشانگر ماشین تحریر |
| `--font-weight-regular`  | `400` | متن بدنه پیش‌فرض                        |
| `--font-weight-medium`   | `500` | متن‌های نیمه‌برجسته، لینک‌های ناوبری    |
| `--font-weight-semibold` | `600` | عناوین کارت‌ها، دکمه‌ها                 |
| `--font-weight-bold`     | `700` | عناوین اصلی، لوگو                       |
| `--font-weight-black`    | `900` | عناوین سکشن‌ها، عنوان اصلی صفحه (Hero)  |

---

## سیستم فاصله‌گذاری (Spacing System)

سیستم فاصله‌گذاری پروژه بر اساس **شبکه ۴ پیکسلی (4px Grid System)** طراحی شده است. تمامی مقادیر از ضرایب `0.25rem` (معادل ۴ پیکسل) پیروی می‌کنند.

| توکن         | مقدار     | معادل پیکسل | کاربرد معمول                               |
| ------------ | --------- | ----------- | ------------------------------------------ |
| `--space-1`  | `0.25rem` | ۴px         | فاصله‌های بسیار ریز (بین آیکون و متن)      |
| `--space-2`  | `0.5rem`  | ۸px         | فاصله بین آیتم‌های لیست، گپ‌های کوچک       |
| `--space-3`  | `0.75rem` | ۱۲px        | فاصله داخل کارت‌ها، پدینگ لینک‌ها          |
| `--space-4`  | `1rem`    | ۱۶px        | پدینگ پیش‌فرض کانتینرها، فاصله بین بلوک‌ها |
| `--space-5`  | `1.25rem` | ۲۰px        | گپ بین کارت‌ها در گرید                     |
| `--space-6`  | `1.5rem`  | ۲۴px        | پدینگ کارت‌ها، فاصله بین سکشن‌ها           |
| `--space-8`  | `2rem`    | ۳۲px        | فاصله بین بلوک‌های بزرگ                    |
| `--space-10` | `2.5rem`  | ۴۰px        | فاصله بین سکشن‌های اصلی                    |
| `--space-12` | `3rem`    | ۴۸px        | پدینگ کانتینر در دسکتاپ بزرگ               |
| `--space-16` | `4rem`    | ۶۴px        | فاصله‌های بزرگ (بالای فوتر)                |
| `--space-18` | `6rem`    | ۹۶px        | فاصله‌های بسیار بزرگ (نوار نقل‌قول)        |

### توکن‌های ساختاری

| توکن                  | مقدار                      | کاربرد                                   |
| --------------------- | -------------------------- | ---------------------------------------- |
| `--header-height`     | `70px` → `80px` → `85px`   | ارتفاع هدر (وابسته به بریکپوینت)         |
| `--container-padding` | `--space-4` → `--space-12` | پدینگ افقی کانتینر (وابسته به بریکپوینت) |
| `--max-width`         | `1280px` → `1440px`        | حداکثر عرض محتوای اصلی                   |

> **قانون طلایی:** برای حفظ ریتم بصری، همواره از مقادیر تعریف‌شده در این جدول استفاده کنید. از مقادیر دلخواه (مانند `13px` یا `0.83rem`) اجتناب نمایید.

---

## شعاع‌های گوشه (Border Radius)

| توکن            | مقدار            | کاربرد                                |
| --------------- | ---------------- | ------------------------------------- |
| `--radius-sm`   | `0.25rem` (۴px)  | عناصر بسیار کوچک                      |
| `--radius-md`   | `0.5rem` (۸px)   | آیتم‌های لیست، ورودی‌ها               |
| `--radius-lg`   | `0.75rem` (۱۲px) | کارت‌های FAQ، آیتم‌های جدول موبایل    |
| `--radius-xl`   | `1rem` (۱۶px)    | کارت‌های خدمات، جدول ساعات            |
| `--radius-2xl`  | `1.5rem` (۲۴px)  | قاب تصویر اصلی (Hero)                 |
| `--radius-full` | `9999px`         | دکمه‌ها، نشان‌ها، آواتارها (شکل قرصی) |

---

## انتقال‌ها (Transitions)

| توکن                  | مقدار        | کاربرد                                   |
| --------------------- | ------------ | ---------------------------------------- |
| `--transition-fast`   | `150ms ease` | هاورهای سریع (پس‌زمینه ردیف جدول)        |
| `--transition-normal` | `300ms ease` | انتقال‌های عمومی (کارت‌ها، دکمه‌ها، منو) |
| `--transition-slow`   | `500ms ease` | تغییر تم، انیمیشن‌های بزرگ               |

> **نکته دسترس‌پذیری:** تمامی انیمیشن‌ها و انتقال‌ها با رعایت `prefers-reduced-motion` طراحی شده‌اند. در صورت فعال بودن این تنظیم در سیستم عامل کاربر، تمامی انیمیشن‌ها به حداقل زمان (`0.01ms`) کاهش می‌یابند.

---

## لایه‌بندی (Z-Index Scale)

مدیریت لایه‌های بصری از طریق یک مقیاس عددی متمرکز انجام می‌شود:

| توکن           | مقدار  | عنصر مرتبط          |
| -------------- | ------ | ------------------- |
| `--z-loading`  | `1000` | صفحه بارگذاری اولیه |
| `--z-header`   | `100`  | سربرگ شناور         |
| `--z-menu`     | `90`   | منوی ناوبری موبایل  |
| `--z-float`    | `50`   | دکمه تغییر تم       |
| `--z-back-top` | `40`   | دکمه بازگشت به بالا |

> **قانون:** نوار پیشرفت مطالعه با `calc(var(--z-header) + 1)` دقیقاً بالای هدر قرار می‌گیرد. از مقادیر `z-index` دلخواه خارج از این مقیاس استفاده نکنید.

---

## سایه‌ها (Shadows)

### تم روشن

| توکن          | مقدار                                | کاربرد                    |
| ------------- | ------------------------------------ | ------------------------- |
| `--shadow-sm` | `0 1px 3px rgba(74, 58, 26, 0.08)`   | سایه‌های ظریف             |
| `--shadow-md` | `0 6px 14px rgba(74, 58, 26, 0.1)`   | کارت‌ها، هدر              |
| `--shadow-lg` | `0 14px 28px rgba(74, 58, 26, 0.13)` | هاور کارت‌ها، منوی موبایل |
| `--shadow-xl` | `0 24px 44px rgba(74, 58, 26, 0.16)` | قاب تصویر، هاور نهایی     |

### تم تاریک

| توکن            | مقدار                               | کاربرد                           |
| --------------- | ----------------------------------- | -------------------------------- |
| `--shadow-sm`   | `0 1px 3px rgba(0, 0, 0, 0.5)`      | سایه‌های ظریف                    |
| `--shadow-md`   | `0 6px 14px rgba(0, 0, 0, 0.5)`     | کارت‌ها، هدر                     |
| `--shadow-lg`   | `0 14px 28px rgba(0, 0, 0, 0.6)`    | هاور کارت‌ها                     |
| `--shadow-xl`   | `0 24px 44px rgba(0, 0, 0, 0.7)`    | قاب تصویر                        |
| `--shadow-glow` | `0 0 24px rgba(230, 193, 92, 0.14)` | هاله نور کهربایی (ویژه تم تاریک) |

> **نکته:** در تم تاریک، سایه‌ها شدت بیشتری دارند (شفافیت بالاتر) تا عمق بصری در پس‌زمینه تیره حفظ شود. سایه `--shadow-glow` صرفاً در تم تاریک فعال است و برای ایجاد افکت نورانی ملایم حول عناصر تأکیدی استفاده می‌شود.

---

## شکست‌های واکنش‌گرا (Responsive Breakpoints)

### استراتژی: موبایل-اول (Mobile-First)

تمامی استایل‌های پایه برای **کوچک‌ترین عرض صفحه** (موبایل) نوشته شده‌اند و در عرض‌های بزرگ‌تر، ویژگی‌های جدید **اضافه** می‌شوند (نه بازنویسی). این رویکرد حجم کد را کاهش داده و عملکرد را در دستگاه‌های کم‌توان بهبود می‌بخشد.

### نقاط شکست (Breakpoints)

| بریکپوینت         | عرض        | توکن تغییر یافته                                             | تغییرات کلیدی                                             |
| ----------------- | ---------- | ------------------------------------------------------------ | --------------------------------------------------------- |
| **پایه (موبایل)** | `< 480px`  | `--container-padding: --space-4`                             | چیدمان تک‌ستونه، منوی همبرگری، جدول کارتی                 |
| **sm**            | `≥ 480px`  | `--container-padding: --space-5`                             | دکمه‌های CTA در کنار هم (ردیفی)                           |
| **md**            | `≥ 640px`  | `--container-padding: --space-6`<br>`--header-height: 80px`  | گرید خدمات ۲ ستونه، فوتر ۲ ستونه، جدول کامل               |
| **lg**            | `≥ 768px`  | `--container-padding: --space-8`                             | معرفی ۲ ستونه (متن + تصویر)، قوانین عضویت ۲ ستونه         |
| **xl**            | `≥ 1024px` | `--container-padding: --space-10`<br>`--header-height: 85px` | منوی افقی (حذف همبرگری)، گرید خدمات ۳ ستونه، فوتر ۴ ستونه |
| **2xl**           | `≥ 1280px` | `--container-padding: --space-12`                            | عنوان Hero به سایز 5xl، پدینگ بیشتر کارت‌ها               |
| **3xl**           | `≥ 1600px` | `--max-width: 1440px`                                        | افزایش حداکثر عرض کانتینر                                 |

### خلاصه تغییرات چیدمان در هر بریکپوینت

```
موبایل (< 480px)
├── چیدمان: تک‌ستونه
├── ناوبری: همبرگری (آکاردئونی)
├── جدول ساعات: کارتی (بدون هدینگ)
└── فوتر: تک‌ستونه

تبلت (640px - 1023px)
├── چیدمان: ۲ ستونه (خدمات، فوتر، قوانین)
├── ناوبری: همبرگری
├── جدول ساعات: کامل با هدینگ
└── معرفی: ۲ ستونه (متن + تصویر)

دسکتاپ (≥ 1024px)
├── چیدمان: ۳ ستونه (خدمات)، ۴ ستونه (فوتر)
├── ناوبری: افقی (حذف همبرگری)
├── جدول ساعات: کامل
└── معرفی: ۲ ستونه با فاصله بیشتر
```

</div>
